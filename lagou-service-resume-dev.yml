spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/travel?useUnicode=true&&characterEncoding=utf-8&&serverTimezone=UTC&&useSSL=true
    username: root
    password: root
  jpa:
    database: MySQL
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
lagou:
  message: hello  lagou 100!
#避免将驼峰命名转换为下划线命名
